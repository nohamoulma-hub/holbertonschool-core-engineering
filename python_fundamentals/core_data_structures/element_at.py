#!/usr/bin/env python3
def element_at(my_list, idx):
    for idx in range(my_list):
        if idx < my_list or idx > my_list:
            return None
        else:
            print("{}".format(idx))
